(ns suse.obs.vendor-helm-test
  (:require [clojure.test :refer [deftest is testing]]
            [suse.obs.vendor-helm :as sut])) ; system under test

(deftest a-test
  (testing "Dummy test"
    (is (= 1 1))))
